// Copyright (c) 2020 Sendanor. All rights reserved.

export enum RootRoutePath {
    INDEX = "/",
    LOGIN = "/login",
    LOGOUT = "/logout",
    DOMAINS = "/domains",
}

export enum DomainRoutePath {
    INDEX = "/",
    HOSTS = "/hosts",
    GROUPS = "/groups",
}